spring:
  datasource:
    url: jdbc:h2:mem:testdb
  sql:
    init:
      mode: always
#  shell:
#    interactive:
#      enabled: false

  jpa:
    generate-ddl: true
    hibernate:
      ddl-auto: validate

    show-sql: true

  main:
    allow-circular-references: true